#!/usr/bin/env ruby
require '%{gem_name}'
require '%{gem_name}/commands'

runner = %{gem_name_camel}::CLI.runner

begin
  exit runner.run ARGV
rescue => e
  puts e.backtrace.reverse if ENV['DEBUG']
  say! "!txtred!#{e.class}: #{e.message}"
  exit 1
end